apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry-proxy-controller
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chart.labels" . | nindent 4 }}
    control-plane: controller-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "chart.selectorLabels" . | nindent 6 }}
      control-plane: controller-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: registry-proxy-controller
      labels:
        {{- include "chart.labels" . | nindent 8 }}
        control-plane: controller-manager
        sidecar.istio.io/inject: "false"
        {{- if and .Values.controllerManager.pod .Values.controllerManager.pod.labels }}
        {{- range $key, $value := .Values.controllerManager.pod.labels }}
        {{ $key }}: {{ $value }}
        {{- end }}
        {{- end }}
    spec:
      containers:
        - name: registry-proxy-controller
          args:
            {{- range .Values.controllerManager.container.args }}
            - {{ . }}
            {{- end }}
          command:
          image: "{{ .Values.global.images.registry_proxy }}"
          imagePullPolicy: IfNotPresent
          env:
            - name: PROXY_IMAGE
              value: "{{ .Values.global.images.connection }}"
            {{- range $key, $value := .Values.controllerManager.container.env }}
            - name: {{ $key }}
              value: {{ $value }}
            {{- end }}
            {{- if .Values.global.proxy.url }}
            - name: PROXY_URL
              value: "{{ .Values.global.proxy.url }}"
            {{- end }}
            {{- if .Values.global.proxy.locationID }}
            - name: PROXY_LOCATION_ID
              value: "{{ .Values.global.proxy.locationID }}"
            {{- end }}
          livenessProbe:
            initialDelaySeconds: 15
            periodSeconds: 20
            httpGet:
              path: /healthz
              port: 8081
          readinessProbe:
            initialDelaySeconds: 5
            periodSeconds: 10
            httpGet:
              path: /readyz
              port: 8081
          resources:
            limits:
              cpu: 500m
              memory: 128Mi
            requests:
              cpu: 5m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            capabilities:
              drop:
                - "ALL"
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: {{ .Values.controllerManager.serviceAccountName }}
      terminationGracePeriodSeconds: 10
