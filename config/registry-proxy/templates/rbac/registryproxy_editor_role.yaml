{{- if .Values.rbac.enable }}
# permissions for end users to edit registryproxies.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  name: registryproxy-editor-role
rules:
  - apiGroups:
      - operator.kyma-project.io
    resources:
      - registryproxies
    verbs:
      - create
      - delete
      - get
      - list
      - patch
      - update
      - watch
  - apiGroups:
      - operator.kyma-project.io
    resources:
      - registryproxies/status
    verbs:
      - get
{{- end -}}
