# [MANAGER]: Manager Deployment Configurations
controllerManager:
  container:
    image:
      repository: image-pull-reverse-proxy-controller
      tag: latest
    args:
      - "--leader-elect"
      - "--metrics-bind-address=:8443"
      - "--health-probe-bind-address=:8081"
    env:
      PROXY_IMAGE: "rebelthor/sleep:latest" #TODO: change to  image:latest or sth
      PROXY_COMMAND: "/sleep.sh" # TODO; change to /reverse-proxy
  serviceAccountName: image-pull-reverse-proxy-controller-manager

# [RBAC]: To enable RBAC (Permissions) configurations - this field is expected by the autogenerated files
rbac:
  enable: true

# [CRDs]: To enable the CRDs - this field is expected by the autogenerated files
crd:
  enable: true

  # Enabling this option adds the "helm.sh/resource-policy": keep
  # annotation to the CRD, ensuring it remains installed even when
  # the Helm release is uninstalled.
  # NOTE: Removing the CRDs will also remove all cert-manager CR(s)
  # (Certificates, Issuers, ...) due to garbage collection.
  keep: true

# [METRICS]: Set to true to generate manifests for exporting metrics.
# To disable metrics export set false, and ensure that the
# ControllerManager argument "--metrics-bind-address=:8443" is removed.
metrics:
  enable: true

# [PROMETHEUS]: To enable a ServiceMonitor to export metrics to Prometheus set true
prometheus:
  enable: false

# [CERT-MANAGER]: To enable cert-manager injection to webhooks set true
certmanager:
  enable: false

# [NETWORK POLICIES]: To enable NetworkPolicies set true
networkPolicy:
  enable: false
