kind: component
metadata:
  name: image-pull-request-proxy
  displayName: Image Pull Request Proxy
  description: Enables kyma users to setup a managed connection between kubelet and an onprem docker registry
  tags:
    - kyma
spec:
  type: operator
  team:
    name: otters
  ci:
    pipelineLinks:
      - name: Azure DevOps pipelne
        url: https://dev.azure.com/hyperspace-pipelines/kyma/_build?definitionId=18059&_a=summary
